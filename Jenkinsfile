#!/usr/bin/env groovy
pipeline {
	agent any
    stages {
       stage('Build') { 
             echo 'start build'
            }
        }
        stage('Deploy') {
            steps {
               echo 'start deploy'
            }
        }
    }
}
