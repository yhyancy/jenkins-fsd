#!/usr/bin/env groovy
pipeline {
	agent any
    stages {
       stage('Build') { 
	       steps {
             	echo 'start build'
	       }
       }
       stage('Deploy') {
	       steps{
		echo 'start deploy'
	       }
        }
    }
}
